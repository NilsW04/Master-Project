\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{theorem-boxes}[2026/08/19 Theorem and definition boxes]

% Das Paket lädt seine eigenen Abhängigkeiten und kann dadurch auch unabhängig
% von template.cls verwendet werden.
\RequirePackage[many]{tcolorbox}
\RequirePackage{etoolbox}

% ---------------------------------------------------------------------------
% Zähler
% ---------------------------------------------------------------------------
\newcounter{definitioncounter}
\newcounter{sentencecounter}
\newcounter{lemmacounter}
\newcounter{theoremcounter}
\newcounter{korollarcounter}
\newcounter{samplecounter}
\newcounter{proofcounter}
\newcounter{blockcounter}

% ---------------------------------------------------------------------------
% Eigene Inhaltsverzeichnisse
% ---------------------------------------------------------------------------
\makeatletter

\newcommand{\listofdefinitions}{%
  \section*{Inhaltsverzeichnis der Definitionen}
  \@starttoc{lod}
}

\newcommand{\listofsentences}{%
  \section*{Inhaltsverzeichnis der Sätze}
  \@starttoc{los}
}

\newcommand{\listoflemmata}{%
  \section*{Inhaltsverzeichnis der Lemmata}
  \@starttoc{lem}
}

\newcommand{\listoftheorems}{%
  \section*{Inhaltsverzeichnis der Theoreme}
  \@starttoc{teo}
}

\newcommand{\listofkorollars}{%
  \section*{Inhaltsverzeichnis der Korollare}
  \@starttoc{kor}
}

\newcommand{\listofsamples}{%
  \section*{Inhaltsverzeichnis der Beispiele}
  \@starttoc{sam}
}

\newcommand{\listofproofs}{%
  \section*{Inhaltsverzeichnis der Beweise}
  \@starttoc{pro}
}

\makeatother

% ---------------------------------------------------------------------------
% Boxen
% ---------------------------------------------------------------------------
\newtcolorbox[auto counter, number within=section]{sentence}[2][]{%
  tikznode boxed title,
  enhanced,
  breakable,
  arc=0mm,
  interior style={white},
  attach boxed title to top center={yshift=-\tcboxedtitleheight/2},
  fonttitle=\bfseries,
  colbacktitle=white,
  coltitle=black,
  boxed title style={size=normal, colframe=white, boxrule=0pt},
  title={Satz%
    \ifstrempty{#2}{}{~-~#2}%
  },
  label={sent:#1},
  before upper={%
    \refstepcounter{sentencecounter}%
    \addcontentsline{los}{subsection}{Satz%
      \ifstrempty{#2}{}{~-~#2}%
    }%
  }
}

\newtcolorbox[auto counter, number within=section]{definition}[2][]{%
  tikznode boxed title,
  enhanced,
  breakable,
  arc=0mm,
  interior style={white},
  attach boxed title to top center={yshift=-\tcboxedtitleheight/2},
  fonttitle=\bfseries,
  colbacktitle=white,
  coltitle=black,
  boxed title style={size=normal, colframe=white, boxrule=0pt},
  title={Definition%
    \ifstrempty{#2}{}{~-~#2}%
  },
  label={def:#1},
  before upper={%
    \refstepcounter{definitioncounter}%
    \addcontentsline{lod}{subsection}{Definition%
      \ifstrempty{#2}{}{~-~#2}%
    }%
  }
}

\newtcolorbox[auto counter, number within=section]{lemma}[2][]{%
  tikznode boxed title,
  enhanced,
  breakable,
  arc=0mm,
  interior style={white},
  attach boxed title to top center={yshift=-\tcboxedtitleheight/2},
  fonttitle=\bfseries,
  colbacktitle=white,
  coltitle=black,
  boxed title style={size=normal, colframe=white, boxrule=0pt},
  title={Lemma%
    \ifstrempty{#2}{}{~-~#2}%
  },
  label={lem:#1},
  before upper={%
    \refstepcounter{lemmacounter}%
    \addcontentsline{lem}{subsection}{Lemma%
      \ifstrempty{#2}{}{~-~#2}%
    }%
  }
}

\newtcolorbox[auto counter, number within=section]{theorem}[2][]{%
  tikznode boxed title,
  enhanced,
  breakable,
  arc=0mm,
  interior style={white},
  attach boxed title to top center={yshift=-\tcboxedtitleheight/2},
  fonttitle=\bfseries,
  colbacktitle=white,
  coltitle=black,
  boxed title style={size=normal, colframe=white, boxrule=0pt},
  title={Theorem%
    \ifstrempty{#2}{}{~-~#2}%
  },
  label={theo:#1},
  before upper={%
    \refstepcounter{theoremcounter}%
    \addcontentsline{teo}{subsection}{Theorem%
      \ifstrempty{#2}{}{~-~#2}%
    }%
  }
}

\newtcolorbox[auto counter, number within=section]{korollar}[2][]{%
  tikznode boxed title,
  enhanced,
  breakable,
  arc=0mm,
  interior style={white},
  attach boxed title to top center={yshift=-\tcboxedtitleheight/2},
  fonttitle=\bfseries,
  colbacktitle=white,
  coltitle=black,
  boxed title style={size=normal, colframe=white, boxrule=0pt},
  title={Korollar%
    \ifstrempty{#2}{}{~-~#2}%
  },
  label={kor:#1},
  before upper={%
    \refstepcounter{korollarcounter}%
    \addcontentsline{kor}{subsection}{Korollar%
      \ifstrempty{#2}{}{~-~#2}%
    }%
  }
}

\newtcolorbox[auto counter, number within=section]{sample}[2][]{%
  tikznode boxed title,
  enhanced,
  breakable,
  arc=0mm,
  interior style={white},
  attach boxed title to top center={yshift=-\tcboxedtitleheight/2},
  fonttitle=\bfseries,
  colbacktitle=white,
  coltitle=black,
  boxed title style={size=normal, colframe=white, boxrule=0pt},
  title={Beispiel%
    \ifstrempty{#2}{}{~-~#2}%
  },
  label={sample:#1},
  before upper={%
    \refstepcounter{samplecounter}%
    \addcontentsline{sam}{subsection}{Beispiel%
      \ifstrempty{#2}{}{~-~#2}%
    }%
  }
}

\newtcolorbox[auto counter, number within=section]{proof}[2][]{%
  tikznode boxed title,
  enhanced,
  breakable,
  arc=0mm,
  interior style={white},
  attach boxed title to top center={yshift=-\tcboxedtitleheight/2},
  fonttitle=\bfseries,
  colbacktitle=white,
  coltitle=black,
  boxed title style={size=normal, colframe=white, boxrule=0pt},
  title={Beweis%
    \ifstrempty{#2}{}{~-~#2}%
  },
  label={proof:#1},
  before upper={%
    \refstepcounter{proofcounter}%
    \addcontentsline{pro}{subsection}{Beweis%
      \ifstrempty{#2}{}{~-~#2}%
    }%
  }
}

\newtcolorbox[auto counter, number within=section]{block}[2][]{%
  tikznode boxed title,
  enhanced,
  breakable,
  arc=0mm,
  interior style={white},
  attach boxed title to top center={yshift=-\tcboxedtitleheight/2},
  fonttitle=\bfseries,
  colbacktitle=white,
  coltitle=black,
  boxed title style={size=normal, colframe=white, boxrule=0pt},
  title={%
    \ifstrempty{#2}{}{#2}%
  },
  label={block:#1},
  before upper={%
    \refstepcounter{blockcounter}%
  }
}

\endinput
